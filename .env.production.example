# Checkout context: local-development or remote-production.
WILDEDITOR_ENVIRONMENT=remote-production

# Production settings are service-specific and supplied by GitHub Actions or
# the deployment platform's secret store. This file is intentionally not a
# runnable aggregate environment.
#
# Use the templates under apps/* and verify the current consumers in source.
# See docs/deployment.md and docs/configuration.md.
