# Frontend Production Environment Configuration
# Luminari Wilderness Editor - Production Settings
# These variables should be set in your production deployment (Netlify, Vercel, etc.)

# Backend API URL (HTTPS required for production)
VITE_API_URL=https://api.wildedit.luminarimud.com/api

# API Key for destructive operations (POST, PUT, DELETE)
# This should be the same key configured on your backend server
VITE_WILDEDITOR_API_KEY=your_production_api_key_here

# Supabase Configuration (production project)
# Get these values from your production Supabase project dashboard
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your_production_supabase_anon_key

# Production Settings
VITE_ENABLE_DEBUG=false
VITE_MOCK_API=false
VITE_DISABLE_AUTH=false
