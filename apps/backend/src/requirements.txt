fastapi==0.141.1
uvicorn[standard]==0.52.1
sqlalchemy==2.0.51
pymysql==1.2.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
cryptography==50.0.0  # Required for pymysql with some MySQL versions
geoalchemy2==0.20.0  # For spatial data types (POLYGON, LINESTRING)

# Production dependencies
gunicorn==26.0.0  # Production WSGI server
requests==2.34.2  # For health checks
python-multipart==0.0.32  # For file uploads if needed

# Testing dependencies
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1  # For async testing with FastAPI
httpx2==2.9.1  # Current Starlette/FastAPI TestClient transport

# Optional monitoring and logging
sentry-sdk[fastapi]==2.66.1  # Error tracking
prometheus-client==0.26.0  # Metrics collection
